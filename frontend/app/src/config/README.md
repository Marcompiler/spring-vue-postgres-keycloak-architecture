# Frontend - config

## Description

Here is **the frontend configuration**.

It includes:

- The [**Keycloak configuration**](./keycloak.js): the URL of the Keycloak instance, along with the realm name and the application's client ID.
- The [**backend configuration**](./backend.js): the URL pointing to the Spring API.
